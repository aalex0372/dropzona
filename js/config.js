/**
 * DROPZONE — App config (limits, DOM ids).
 * Central place for magic numbers and element ids used by feed/router.
 */

/** Max feed rows before trimming (viewer feed) */
export const FEED_VIEWER_MAX = 15;

/** Max feed rows before trimming (streamer feed) */
export const FEED_STREAMER_MAX = 12;

/** Number of wizard steps */
export const WIZARD_STEPS = 4;

/** Viewer feed list element id */
export const FEED_LIST_ID = 'feedList';

/** Streamer feed list element id */
export const S_FEED_LIST_ID = 'sFeedList';

/** Viewer feed count badge element id */
export const FEED_COUNT_ID = 'feedCount';
